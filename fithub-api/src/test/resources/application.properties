# --- Configuração H2 para Testes ---
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# Diz ao Hibernate para criar as tabelas
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false

# Força o Spring a esperar o Hibernate criar as tabelas antes de rodar o data.sql
spring.jpa.defer-datasource-initialization=true

# Chave secreta para assinar os tokens JWT
api.security.token.secret=secretKeyFitHubApiChaveApiFitHub2025Segura